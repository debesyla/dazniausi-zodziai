name: Verify application

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run check

      - name: Test
        run: npm test

      - name: Install browser matrix for release tests
        run: npx playwright install --with-deps chromium firefox webkit

      - name: Test production browser matrix
        id: browser-tests
        env:
          BASE_PATH: /lietuviu-zodziai
          PUBLIC_SITE_URL: https://example.test/lietuviu-zodziai
        run: npm run test:browser

      - name: Upload required browser report
        if: ${{ !cancelled() && steps.browser-tests.outcome != 'skipped' }}
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: playwright-report/index.html
          if-no-files-found: error

      - name: Upload optional browser diagnostics
        if: ${{ !cancelled() && steps.browser-tests.outcome != 'skipped' }}
        uses: actions/upload-artifact@v7
        with:
          name: playwright-test-results
          path: test-results/
          if-no-files-found: ignore

      - name: Verify checked-in public data products
        run: npm run products:verify

      - name: Verify public provenance boundaries
        run: npm run public:verify

      - name: Production build
        env:
          NODE_ENV: production
        run: npm run build

      - name: Verify portable subpath build
        env:
          BASE_PATH: /lietuviu-zodziai
          PUBLIC_SITE_URL: https://example.test/lietuviu-zodziai
        run: npm run build
